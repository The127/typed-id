[package]
name = "typed-id"
version = "0.1.0"
edition = "2024"
description = "Proc-macros for generating typed UUID and slug newtype wrappers"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
